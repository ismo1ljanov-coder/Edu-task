VITE_API_BASE_URL=http://localhost:4000/api

# Real So'zdon (Word-Box-Game) site — the student "So'zdon orqali bajarish"
# button opens this URL in a new tab with the homework id as a query param.
# Backend API integration with So'zdon is still mock-only (see /api/sozdon/*).
VITE_SOZDON_URL=https://sozdon.uz
